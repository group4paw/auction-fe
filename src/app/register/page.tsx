import Register from "@/components/Register/Register";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register | Easybid",
  description: "...",
};

const RegisterPage = () => {
  return <Register />;
};

export default RegisterPage;
